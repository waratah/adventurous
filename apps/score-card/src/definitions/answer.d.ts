/*** an answer to a single question that will be method. to question text. */

export interface answer {
  code: string;
  done?: boolean;
  doneDate: Date;
  verified?: boolean;
  mappedCode?: string;
}
